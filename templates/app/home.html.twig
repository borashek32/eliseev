{% extends 'base.html.twig' %}

{% block title %}Home{% endblock %}

{% block breadcrumbs %}
    <ol class="breadcrumb">


        {% if is_granted('ROLE_ADMI') %}
            <li class="breadcrumb-item active">Hello, admin<br>{{ app.user.email }}</li>
        {% elseif is_granted('ROLE_USER') %}
            <li class="breadcrumb-item active">Hello, user<br>{{ app.user.email }}</li>
        {% else %}
            <li class="breadcrumb-item active">Hello</li>
        {% endif %}
    </ol>
{% endblock %}

{% block body %}

{#    {{ work_projects_calendar() }}#}

    <div class="row">
        <div class="col-md-6">
{#            {{ work_projects_own_tasks() }}#}
        </div>
        <div class="col-md-6">
{#            {{ work_projects_tasks_for_me() }}#}
        </div>
    </div>

{% endblock %}